services:
  - type: web
    name: electrobit-backend
    env: node
    rootDir: backend
    buildCommand: yarn install && yarn build
    startCommand: node dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        value: file:./dev.db
